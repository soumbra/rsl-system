spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
      
logging:
  level:
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    com.rslsystem.api: DEBUG

# CORS para desenvolvimento
cors:
  allowed-origins: 
    - "http://localhost:3000"
    - "http://localhost:5173"